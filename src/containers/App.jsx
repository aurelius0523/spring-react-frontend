import React from "react";
import { format } from "url";

export default class App extends React.Component {
  render() {
    return (
      <form id="dummyFrom">
        <label>A from!</label>
        <input type="text" />
      </form>
    );
  }
}
